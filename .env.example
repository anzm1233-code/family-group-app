# Local Postgres connection string for `npm run dev:api`.
# In production, Netlify injects the equivalent NETLIFY_DB_URL automatically
# once the site is linked and `netlify db init` has provisioned Netlify DB.
NETLIFY_DB_URL=postgres://appuser:apppass@127.0.0.1:5432/familygroupapp
